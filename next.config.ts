/** @type {import('next').NextConfig} */const nextConfig = {
  images: {
    domains: ['aceternity.com', 'i.scdn.co', 'assets.aceternity.com', 'raw.githubusercontent.com', 'cdn.thestorygraph.com'],
  },
  // other configurations if any
};

export default nextConfig;  